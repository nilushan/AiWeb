{
  "name": "aiweb-monorepo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "AiWeb - Monorepo with Astro static site and Keystatic CMS",
  "workspaces": [
    "apps/web",
    "apps/cms"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:cms": "npm run dev --workspace=apps/cms",
    "build": "npm run build:web && npm run build:cms",
    "build:web": "npm run build --workspace=apps/web",
    "build:cms": "npm run build --workspace=apps/cms",
    "preview:web": "npm run preview --workspace=apps/web",
    "clean": "rm -rf apps/web/dist apps/web/.astro apps/cms/dist node_modules apps/*/node_modules",
    "install:all": "npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

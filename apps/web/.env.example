# ===================================
# Local Development Configuration
# ===================================
# Copy this file to .env and update with your values
# Astro automatically loads .env files - no additional setup needed!

# Site Configuration
SITE_URL=http://localhost:4321
BASE_PATH=/

# GitHub Repository (for Keystatic CMS)
GITHUB_REPO_OWNER=nilushan
GITHUB_REPO_NAME=AiWeb

# GitHub Personal Access Token (REQUIRED for local Keystatic admin)
# Create at: https://github.com/settings/tokens
# Needs 'repo' scope to read/write content
# Leave empty to use GitHub OAuth (will require browser login)
GITHUB_TOKEN=

# ===================================
# Production Configuration
# ===================================
# These are set via GitHub Actions / Cloud Run, not needed locally

# Production site URL (set by deployment)
# SITE_URL=https://your-site.web.app

# CMS API URL (only if using cloud mode - not needed for GitHub mode)
# PUBLIC_CMS_API_URL=https://your-cms-service.run.app/api/keystatic

# Analytics (optional)
PUBLIC_GA_ID=

# Environment variables for local CMS Docker testing
# Copy this to .env.cms.local and update with your values

NODE_ENV=production
HOST=0.0.0.0
PORT=8080

# GitHub Repository Configuration
GITHUB_REPO_OWNER=nilushan
GITHUB_REPO_NAME=AiWeb

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Needs 'repo' scope for content management
GITHUB_TOKEN=ghp_your_token_here

# Optional: For cloud mode (not needed if using GitHub mode)
# PUBLIC_CMS_API_URL=
